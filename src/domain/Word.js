export default class Word {
  constructor(id, name, transcription, description) {
    this.id = id;
    this.name = name;
    this.transcription = transcription;
    this.description = description;
  }
}
